services:
  presence:
    build: .
    image: presence-ihost:latest
    container_name: presence-ihost
    restart: unless-stopped
    network_mode: host
    cap_add:
      - NET_RAW
    ports:
      - "8082:8082"
    volumes:
      - ./data:/app/data